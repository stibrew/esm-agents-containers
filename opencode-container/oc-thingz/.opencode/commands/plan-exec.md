---
description: Autonomously execute a plan file task by task with subagents, multi-phase reviews, and finalize
---
Invoke the `exec` skill (load it with the skill tool) and execute the plan: $ARGUMENTS

If no plan path was given, follow the skill's step 1 to locate one.
